# Origin

Extracted from the Agent Foundry package blueprint importer, parser, and runtime composer inside the historical Python workbench. The original implementation included a large Tk editing surface and a permissive text grammar. This standalone version isolates the record/blueprint/runtime boundary and tightens it into a deterministic round-trip contract.
